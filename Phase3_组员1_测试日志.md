# Phase3 组员1 — 跨日趋势侦探 · 测试日志

> 测试人：组员1
> 开始时间：2026-06-02
> 原则：每完成一个文件立即测试，记录结果；失败需写原因分析

---

## [2026-06-02 20:00] 环境检查

- **测试方案**：确认 PostgreSQL 运行 + module-b/module-c 依赖可用
- **测试数据**：`SELECT COUNT(*) FROM briefings` 检查是否有 7 天以上简报数据
- **测试结果**：待填写
- **原因分析**：待填写

---

## [2026-06-02 20:30] 测试1: trend_agent.py 单元测试 — mock 数据

- **测试方案**：构造 7 天 mock 简报数据（14 条 morning+evening），调用 `analyze_trends(pool=None, days=7)` 的 mock 回退路径
- **测试数据**：
  ```python
  # 7 天 mock 数据概要：
  # Day1(05-27): LLM=3, Agent=1, 开源=5, RAG=5, 体育=2
  # Day2(05-28): LLM=5, Agent=2, 开源=4, RAG=4, 体育=1
  # Day3(05-29): LLM=5, Agent=3, 开源=3, RAG=3, 体育=3
  # Day4(05-30): LLM=8, Agent=5, 开源=3, RAG=2, 体育=2
  # Day5(05-31): LLM=9, Agent=5, 开源=2, RAG=2, 体育=2
  # Day6(06-01): LLM=11,Agent=7, 开源=2, RAG=2, 体育=2
  # Day7(06-02): LLM=11,Agent=11,开源=1, RAG=2, 体育=2
  ```
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：rising=[Agent(+1000%), LLM(+267%)], falling=[开源(-80%), RAG(-60%)], agent_insight 包含中文趋势解读

---

## [2026-06-02 21:00] 测试2: trend_agent.py — 集成测试（真实 DB）

- **测试方案**：连真实 PostgreSQL，调用 `analyze_trends(pool)` 真实查询 7 天 briefings 表
- **测试数据**：使用数据库中真实简报数据
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：返回结构完整，rising/falling 反映真实标签变化

---

## [2026-06-02 21:30] 测试3: trends.py API — 路由响应测试

- **测试方案**：启动 module-c 后端 → curl 测试 `/api/trends/weekly` 和 `/api/trends/tag/LLM`
- **测试数据**：
  ```bash
  curl http://localhost:8003/api/trends/weekly
  curl http://localhost:8003/api/trends/tag/LLM
  curl http://localhost:8003/api/trends/tag/NotExist
  ```
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：
  - `/api/trends/weekly` → 200 + JSON 含 period/rising/falling/agent_insight
  - `/api/trends/tag/LLM` → 200 + JSON 含 tag/label_zh/chart/dates
  - `/api/trends/tag/NotExist` → 404

---

## [2026-06-02 22:00] 测试4: main.py 路由注册

- **测试方案**：重启 module-c → `/health` + `/api/trends/weekly` + `/api/tags` 全部访问
- **测试数据**：curl 三个端点
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：三个端点均 200，import 不报错

---

## [2026-06-02 22:30] 测试5: api.js 新增趋势方法

- **测试方案**：在小程序开发者工具控制台调用 `api.getWeeklyTrends()` 和 `api.getTagTrend("LLM")`
- **测试数据**：实际调用返回值
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：返回值结构与后端 API 一致

---

## [2026-06-02 23:00] 测试6: 趋势页面渲染 — 4 文件联调

- **测试方案**：编译小程序 → 手动跳转 pages/trends/trends → 查看页面渲染
- **测试数据**：mock 趋势数据（rising 3条, falling 3条, new_tags 2条）
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：
  - Agent 解读卡片正确显示（紫蓝渐变背景）
  - 飙升榜 3 条，红色 ↑ + 百分比
  - 降温榜 3 条，绿色 ↓ + 百分比
  - 新兴标签 2 条
  - 迷你柱状条可见

---

## [2026-06-02 23:30] 测试7: app.json Tab 注册

- **测试方案**：小程序编译 → 查看底部 TabBar → 点击"趋势" Tab
- **测试数据**：底部导航栏截图
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：底部出现 4 个 Tab（今日简报、历史、趋势、我的），切换正常

---

## [2026-06-03 00:00] 测试8: 全链路联调

- **测试方案**：
  1. 启动全部服务（DB + module-b + module-c）
  2. Dashboard 触发早报管道
  3. 确认 briefings 表有新数据
  4. 小程序打开趋势页
  5. 验证 Agent 解读基于实际数据
- **测试数据**：真实管道生成的简报 → trend_agent 分析 → 小程序展示
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：全链路工作正常，趋势数据与简报内容对应

---

## [2026-06-03 00:30] 测试9: 异常场景

- **测试方案**：
  1. 停止 PostgreSQL → 验证 mock 回退
  2. 停止 module-c → 验证小程序错误提示
  3. 数据不足 7 天 → 验证返回现有数据
- **测试数据**：各种故障注入
- **测试结果**：待填写
- **原因分析**：待填写
- **预期**：所有异常场景有合理降级，不崩溃不白屏

---

## 测试总结

| 测试项 | 状态 | 关键发现 |
|---|---|---|
| 环境检查 | 待填写 | 待填写 |
| 测试1: mock 单元 | 待填写 | 待填写 |
| 测试2: DB 集成 | 待填写 | 待填写 |
| 测试3: API 响应 | 待填写 | 待填写 |
| 测试4: 路由注册 | 待填写 | 待填写 |
| 测试5: api.js | 待填写 | 待填写 |
| 测试6: 页面渲染 | 待填写 | 待填写 |
| 测试7: Tab 注册 | 待填写 | 待填写 |
| 测试8: 全链路 | 待填写 | 待填写 |
| 测试9: 异常场景 | 待填写 | 待填写 |

**最终结论**：待测试完成后填写
